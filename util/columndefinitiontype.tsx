type ColumnDefinitionType = {
    // TODO: proper way is to implement the generic key type K, and link by K. taking a short cut by using the index
    header: string;
}

export default ColumnDefinitionType;